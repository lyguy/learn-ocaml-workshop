(executables (
  (names (fuzzy))
  (libraries (core async re))
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
))

(alias
 ((name DEFAULT)
   (deps (fuzzy.exe))))

(alias
 ((name all)
  (deps ((glob_files *.exe)))))
