;; -*- scheme -*-

(jbuild_version 1)

(library
 ((name problem_16)
  (libraries (base stdio expect_test_helpers_kernel))
  (inline_tests)
  (preprocess (pps (ppx_jane)))
  (flags (:standard -short-paths))
  ))
